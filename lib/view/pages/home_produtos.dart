import 'package:flutter/material.dart';

class HomeProdutos extends StatelessWidget {
  const HomeProdutos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: 
    );
  }
}